---
import Layout from '../../layouts/Layout.astro';
import ChatCreate from '../../components/tools/ChatCreate.svelte';
import { getTranslations, t, type Locale } from '../../lib/i18n';

const locale = (Astro.currentLocale ?? 'en') as Locale;
const dict = getTranslations(locale);

export const prerender = true;
---

<Layout
  title="Create Encrypted Chat — encrypt.click"
  description="Create a private, ephemeral encrypted chatroom. No signup, no logs."
>
  <div class="max-w-lg mx-auto px-5 py-12 md:py-20 text-center">
    <div class="mb-8 space-y-3">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight">
        Encrypted <span class="text-emerald-500">Chat</span>
      </h1>
      <p class="text-sm text-zinc-500 dark:text-zinc-400 leading-relaxed max-w-sm mx-auto">
        End-to-end encrypted. No database. Messages disappear automatically. Share the link to invite others.
      </p>
    </div>
    <ChatCreate client:load />
  </div>
</Layout>

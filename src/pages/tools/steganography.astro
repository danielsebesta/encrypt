---
import Layout from '../../layouts/Layout.astro';
import SteganographyView from '../../components/tools/Steganography.svelte';
import ToolHelp from '../../components/tools/ToolHelp.svelte';
import { getTranslations, t, toolClientDict, type Locale } from '../../lib/i18n';

const locale = (Astro.currentLocale ?? 'en') as Locale;
const dict = getTranslations(locale);
const clientDict = toolClientDict(dict, 'tools.steganography');
---

<Layout
  title={t(dict, 'tools.steganography.meta.title')}
  description={t(dict, 'tools.steganography.meta.description')}
>
  <div class="max-w-4xl mx-auto px-5 py-12 md:py-20">
    <div class="tool-hero">
      <div class="tool-hero__copy">
        <h1 class="tool-hero__title">
          {t(dict, 'tools.steganography.h1')} <span class="text-emerald-500">{t(dict, 'tools.steganography.h1Highlight')}</span>
          <ToolHelp client:idle dict={clientDict} prefix="tools.steganography" />
        </h1>
        <p class="tool-hero__subtitle">{t(dict, 'tools.steganography.subtitle')}</p>
      </div>
    </div>
    <div class="mt-8">
      <SteganographyView client:idle locale={locale} dict={clientDict} />
    </div>
  </div>
</Layout>

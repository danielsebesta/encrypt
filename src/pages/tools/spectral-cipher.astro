---
import Layout from '../../layouts/Layout.astro';
import SpectralCipherView from '../../components/tools/SpectralCipher.svelte';
import ToolHelp from '../../components/tools/ToolHelp.svelte';
import { getTranslations, t, toolClientDict, type Locale } from '../../lib/i18n';

const locale = (Astro.currentLocale ?? 'en') as Locale;
const dict = getTranslations(locale);
const clientDict = toolClientDict(dict, 'tools.spectralCipher');
---

<Layout
  title={t(dict, 'tools.spectralCipher.meta.title')}
  description={t(dict, 'tools.spectralCipher.meta.description')}
>
  <div class="max-w-4xl mx-auto px-5 py-12 md:py-20">
    <div class="tool-hero">
      <div class="tool-hero__copy">
        <h1 class="tool-hero__title">
          {t(dict, 'tools.spectralCipher.h1')} <span class="text-emerald-500">{t(dict, 'tools.spectralCipher.h1Highlight')}</span>
          <ToolHelp client:idle dict={clientDict} prefix="tools.spectralCipher" />
        </h1>
        <p class="tool-hero__subtitle">{t(dict, 'tools.spectralCipher.subtitle')}</p>
      </div>
    </div>
    <div class="mt-8">
      <SpectralCipherView client:idle locale={locale} dict={clientDict} />
    </div>
  </div>
</Layout>

---
import Layout from '../../layouts/Layout.astro';
import BcryptView from '../../components/tools/Bcrypt.svelte';
import ToolHelp from '../../components/tools/ToolHelp.svelte';
import { getTranslations, t, toolClientDict, type Locale } from '../../lib/i18n';

const locale = (Astro.currentLocale ?? 'en') as Locale;
const dict = getTranslations(locale);
const clientDict = toolClientDict(dict, 'tools.bcrypt');
---

<Layout
  title={t(dict, 'tools.bcrypt.meta.title')}
  description={t(dict, 'tools.bcrypt.meta.description')}
>
  <div class="max-w-4xl mx-auto px-5 py-12 md:py-20">
    <div class="tool-hero">
      <div class="tool-hero__copy">
        <h1 class="tool-hero__title">
          {t(dict, 'tools.bcrypt.h1')} <span class="text-emerald-500">{t(dict, 'tools.bcrypt.h1Highlight')}</span>
          <ToolHelp client:idle dict={clientDict} prefix="tools.bcrypt" />
        </h1>
        <p class="tool-hero__subtitle">{t(dict, 'tools.bcrypt.subtitle')}</p>
      </div>
    </div>
    <div class="mt-8">
      <BcryptView client:idle locale={locale} dict={clientDict} />
    </div>
  </div>
</Layout>

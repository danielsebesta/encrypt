---
import Layout from '../../layouts/Layout.astro';
import AnonUploadView from '../../components/tools/AnonUpload.svelte';
import { getTranslations, t, type Locale } from '../../lib/i18n';

const locale = (Astro.currentLocale ?? 'en') as Locale;
const dict = getTranslations(locale);
---

<Layout 
  title={t(dict, 'tools.anonUpload.meta.title')}
  description={t(dict, 'tools.anonUpload.meta.description')}
>
  <div class="max-w-3xl mx-auto px-5 py-12 md:py-20 text-center">
    <div class="mb-12">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tight mb-4">{t(dict, 'tools.anonUpload.h1')} <span class="text-emerald-500">{t(dict, 'tools.anonUpload.h1Highlight')}</span></h1>
      <p class="text-zinc-500 dark:text-zinc-400 text-sm md:text-base leading-relaxed">{t(dict, 'tools.anonUpload.subtitle')}</p>
    </div>
    <div class="card p-8">
      <AnonUploadView client:load locale={locale} />
    </div>
    <div class="mt-12 text-left space-y-4">
      <h3 class="text-xs font-bold uppercase tracking-widest text-zinc-400">{t(dict, 'tools.anonUpload.info.title')}</h3>
      <p class="text-xs text-zinc-500 leading-relaxed">{t(dict, 'tools.anonUpload.info.text')}</p>
    </div>
  </div>
</Layout>

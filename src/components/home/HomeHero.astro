---
import UltimateEncrypt from '../tools/UltimateEncrypt.svelte';
import ChatCreate from '../tools/ChatCreate.svelte';
import { t, type Locale } from '../../lib/i18n';

type Props = {
  locale: Locale;
  dict: Record<string, string>;
};

const { locale, dict } = Astro.props;
---

<section class="home-hero-section">
  <div class="max-w-6xl mx-auto px-5 relative z-1">
    <div class="grid lg:grid-cols-[minmax(0,1fr)_minmax(22rem,28rem)] gap-10 lg:gap-12 items-start">

      <!-- Left column: hero text -->
      <div class="space-y-6 pt-2 md:pt-8" data-aos data-aos-delay="0">
        <h1 class="text-3xl md:text-5xl lg:text-6xl leading-[1.05] tracking-[-0.04em] font-black text-zinc-950 dark:text-zinc-50">
          {t(dict, 'home.hero.title.line1')}
          <br />
          <span class="text-emerald-600 dark:text-emerald-400">{t(dict, 'home.hero.title.line2')}</span>
        </h1>
        <p class="max-w-md text-sm md:text-base leading-7 text-zinc-500 dark:text-zinc-400">
          {t(dict, 'home.hero.subtitle')}
        </p>
        <p class="text-xs text-zinc-400 dark:text-zinc-500 max-w-sm">
          {t(dict, 'home.hero.note')}
        </p>
      </div>

      <!-- Right column: tools -->
      <div class="space-y-4" data-aos data-aos-delay="60">
        <!-- Encrypt widget -->
        <div class="home-tool-panel">
          <p class="home-tool-panel__title">{t(dict, 'home.hero.panelTitle')}</p>
          <div class="home-tool-panel__surface">
            <UltimateEncrypt client:load locale={locale} />
          </div>
        </div>

        <!-- Chat widget -->
        <div class="home-tool-panel">
          <div class="grid sm:grid-cols-[1fr_auto] gap-4 items-start">
            <div>
              <p class="home-tool-panel__title">{t(dict, 'chat.pageTitle')} {t(dict, 'chat.pageTitleHighlight')}</p>
              <ChatCreate client:load locale={locale} />
            </div>
            <p class="text-[10px] text-zinc-400 dark:text-zinc-500 leading-relaxed max-w-[14rem] hidden sm:block pt-6">
              {t(dict, 'chat.pageSubtitle')}
            </p>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>
